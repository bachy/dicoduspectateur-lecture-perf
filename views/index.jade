extends layout

block content
  #root
    canvas#stage
  #images
    img#image(src="/images/image.png")
  script(type="text/javascript").
    var list=!{JSON.stringify(list).replace(/<\//g, "<\\/")};
