extends layout

block content
  #root
    h1= title
    ul#words-list
      each word in list
        li.word
          a.word-link(href="#")=word

script(type="text/javascript").
  var list=!{JSON.stringify(list).replace(/<\//g, "<\\/")};
